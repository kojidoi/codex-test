# Simple Breakout Game

This repository contains a small web-based Breakout game that can be run directly in your browser.

## Running Locally

Open `index.html` in any modern web browser.

## Development

Install dependencies and run tests:

```bash
npm install
npm test
```

A GitHub Actions workflow runs these tests automatically on pushes and pull requests.
